pkgname=Arch_Environment_EAGLE
pkgver=2.5
pkgrel=3
pkgdesc="Environment for my Arch Linux"
arch=('any')
url=""
license=('GPL')
depends=(
    # ─────────────────────────────────────────────
    # System / Boot
    # ─────────────────────────────────────────────
    efibootmgr
    grub
    dosfstools
    pacman-contrib
    base-devel

    # ─────────────────────────────────────────────
    # Networking / Bluetooth / Utilities
    # ─────────────────────────────────────────────
    usbutils
    networkmanager
    openssh
    firefox
    syncthing
    upower      # Optional: `noctalia`
    ddcutil     # Optional: `noctalia`
    bluez       # Optional: `noctalia`
    bluez-utils # Optional: `noctalia`

    # ─────────────────────────────────────────────
    # Desktop / Wayland / Hyprland
    # ─────────────────────────────────────────────
    hyprland
    kitty
    noctalia
    greetd
    greetd-tuigreet

    # ─────────────────────────────────────────────
    # Audio / Media
    # ─────────────────────────────────────────────
    audacious
    playerctl
    pipewire-alsa
    pipewire-jack
    pipewire-pulse

    # ─────────────────────────────────────────────
    # File Manager / Files
    # ─────────────────────────────────────────────
    thunar
    yazi
    aria2
    stow
    udisks2  # Optional: `thunar`
    gvfs-mtp # Optional: `thunar`
    7zip     # Optional: `yazi`
    unzip    # Optional: `lazyvim`

    # ─────────────────────────────────────────────
    # Terminal / Shell
    # ─────────────────────────────────────────────
    zsh
    zsh-autosuggestions
    zsh-completions
    zsh-syntax-highlighting
    bat
    eza
    fd
    fzf
    ripgrep
    zoxide
    fastfetch
    btop
    man-db
    wl-clipboard

    # ─────────────────────────────────────────────
    # Development
    # ─────────────────────────────────────────────
    cmake
    ninja
    git
    neovim
    lazygit

    # ─────────────────────────────────────────────
    # Graphics / Image / Documents
    # ─────────────────────────────────────────────
    imagemagick # Optional: `yazi`
    poppler     # Optional: `yazi`
    resvg       # Optional: `yazi`

    # ─────────────────────────────────────────────
    # Fonts
    # ─────────────────────────────────────────────
    ttf-jetbrains-mono-nerd
)
